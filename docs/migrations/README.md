# Sneezr SQL migrations

Run migrations in **Supabase Dashboard → SQL Editor → New query**. Execute the full contents of each migration file in order.

- **001_sneeze_assessment_enrichment.sql** — Add assessment columns, `updated_at` trigger, and RLS for updates.
